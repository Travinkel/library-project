name: Docker Build & Push

on:
  push:
    branches: [ main, develop ]
    tags:
      - "v*.*.*"

jobs:
  docker:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    steps:
      - uses: actions/checkout@v4

      - name: Login to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build & push API image
        uses: docker/build-push-action@v5
        with:
          context: ./server
          file: ./server/LibraryProject.Api/Dockerfile
          push: true
          tags: |
            ghcr.io/travinkel/library-project/library-api:sha-${{ github.sha }}
            ghcr.io/travinkel/library-project/library-api:${{ github.ref_name == 'main' && 'latest' || 'edge' }}

      - name: Tag release image
        if: startsWith(github.ref, 'refs/tags/')
        run: |
          docker pull ghcr.io/travinkel/library-project/library-api:latest
          docker tag ghcr.io/travinkel/library-project/library-api:latest ghcr.io/travinkel/library-project/library-api:${GITHUB_REF#refs/tags/}
          docker push ghcr.io/travinkel/library-project/library-api:${GITHUB_REF#refs/tags/}